# OPC Gateway Tool 使用說明書

## 簡介

OPC Gateway Tool 把 OPC DA（OLE for Process Control Data Access）伺服器的標籤，透過內建的 OPC UA 伺服器提供給現代的 OPC UA 客戶端。程式常駐執行，會自動重連、自動儲存設定，並可在啟動時自動運作。

## 系統需求

- Windows 10 1903 以上或 Windows 11
- .NET Framework 4.8（新版 Windows 已內建）
- OPC Core Components Redistributable（32 位元），供 OPC DA 通訊
- 可存取的 OPC DA 伺服器

## 安裝

把整個發佈資料夾複製到目標機器，例如 `C:\OPCGateway\`。資料夾內需包含：

```
OPCGatewayTool.exe     主程式
*.dll                  相依函式庫
NLog.config            日誌設定
Config\                設定檔（第一次啟動自動建立）
Logs\                  日誌（自動建立，保留 14 天）
```

第一次啟動 OPC UA 伺服器時會在 `%ProgramData%\OPC Foundation\CertificateStores` 建立應用程式憑證，需要對該目錄有寫入權限。

## 畫面說明

主視窗上方是命令列：**啟動閘道**（F5）、**停止閘道**（Shift+F5）、**儲存設定**（Ctrl+S），以及目前的閘道狀態膠囊：已停止、啟動中、運作中、部分運作、停止中。下方狀態列顯示 DA 與 UA 的連線狀態、運作中／全部標籤數、客戶端數、每秒更新數與錯誤數。

左側四個頁面：

### 監控

- 六張卡片：OPC DA 來源、OPC UA 伺服器（可複製端點 URL）、標籤數、每秒更新數、壞品質與丟棄數、客戶端數與運行時間。
- 標籤表：每列顯示狀態、ItemId、UA 名稱、目前值、品質、型別、時間戳、更新數，以及「啟用」與「可寫」核取方塊。
  - 狀態：待連線（DA 未連線）、等待資料（已訂閱但尚未收到值）、運作中、已停用、錯誤（ItemId 不存在等，滑鼠停留可看原因）。
  - 品質只有異常才上色：不確定為琥珀色、壞品質為紅色。
  - 對列按右鍵可啟用／停用、複製 NodeId、移除標籤。UA 名稱可直接編輯。
- 上方可用關鍵字篩選，或切換「只看異常」。
- 右側列出已連線的 UA 客戶端與其端點、安全模式、身分、訂閱數。

### 標籤

- 按「連線 OPC DA」後左側顯示伺服器的樹狀結構；資料夾展開時才載入子項目。
- 勾選項目後按下方「加入勾選項目」。已加入的項目會顯示綠色勾號與「已加入」。
- 對資料夾按右鍵可「加入此資料夾下的所有項目」（超過 200 個會先確認）。
- 右側可輸入關鍵字搜尋整個伺服器（適合數千個標籤的伺服器），結果可勾選或逐一加入。

### 日誌

- 依等級（追蹤、除錯、資訊、警告、錯誤）與關鍵字篩選，錯誤與警告有計數。
- 自動捲動可關閉；「暫停」可凍結畫面方便閱讀。
- 可匯出成文字檔，或直接開啟 `Logs` 資料夾。完整日誌永遠寫在檔案裡，畫面只保留最近 2000 筆。

### 設定

- **OPC DA 來源**：ProgID（可按「掃描」列舉本機伺服器）、主機、更新頻率、死區、連線逾時、重連間隔、健康檢查間隔、階層分隔符號。
- **OPC UA 伺服器**：伺服器名稱、應用程式名稱、Application URI、連接埠、最大工作階段、根資料夾、命名空間 URI；以及是否以主機名稱作端點、是否鏡射 DA 階層、是否保留無安全性端點、是否啟用安全端點、是否自動信任客戶端憑證。
- **一般**：程式啟動時自動啟動閘道、外觀（跟隨系統、淺色、深色）、設定檔位置、匯入與匯出。命令列右側的月亮／太陽鈕可快速切換深淺色。
- 欄位有錯時會以紅框標示並在上方列出原因。閘道運作中修改連線參數，要重新啟動閘道才會生效。

## 操作流程

1. 設定頁確認 DA 與 UA 參數，套用並儲存。
2. 標籤頁連線 DA，勾選或搜尋要轉換的項目並加入。
3. 按「啟動閘道」。狀態變成「運作中」表示 DA 與 UA 都正常；「部分運作」表示其中一邊失敗，程式會持續重試。
4. 用 UaExpert 連到端點，在 `Objects → Gateway` 下讀取標籤；`Gateway → Status` 下可監看閘道本身的狀態。
5. 要無人值守時，勾選「程式啟動時自動啟動閘道」，並把程式加入 Windows 的啟動項目或工作排程器。

## 常見問題

**掃描找不到伺服器**：確認 OPC Core Components（32 位元）已安裝、OpcEnum 服務存在，且以與 OPC DA 伺服器相同的帳號執行。

**連線逾時**：確認伺服器已啟動、ProgID 正確；遠端連線需完成兩端 DCOM 設定。可在設定頁調高連線逾時。

**UA 客戶端看到 BadNotConnected**：表示 DA 未連線，閘道會依重連間隔自動重試；看日誌頁的原因。

**UA 客戶端看到 BadWaitingForInitialData**：已訂閱但伺服器尚未送出第一筆值，通常在一個更新週期內會變成 Good。

**UA 伺服器無法啟動**：連接埠被占用（換連接埠），或憑證存放區沒有寫入權限（以系統管理員執行一次）。

**UaExpert 顯示憑證警告**：閘道的憑證是自簽憑證，在 UaExpert 信任即可；若關閉了「自動信任客戶端憑證」，也要把 UaExpert 的憑證放到 `UA Applications` 存放區。

**值顯示為 Variant 或無型別**：伺服器未回報原生型別，第一筆值到達後會依實際型別更新。

## 關閉程式

閘道運作中關閉程式會先確認，然後停止 UA 伺服器、中斷 DA、寫回設定。已連線的 UA 客戶端會收到斷線。

---

文件版本 2.0，適用 OPC Gateway Tool 2.0。
